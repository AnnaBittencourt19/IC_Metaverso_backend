# Versões travadas (pip freeze do ambiente local validado em 2026-07-18) para
# evitar quebras silenciosas em novos builds do Docker/Render por resolução de
# dependências divergente. Ao atualizar uma versão, revalidar localmente e
# rodar a suíte de testes antes de commitar.
fastapi==0.136.0
uvicorn==0.44.0
pydantic==2.13.3
python-dotenv==1.2.2

chromadb==1.5.8
langchain==1.2.15
langchain-chroma==1.1.0
langchain-text-splitters==1.1.2
langchain-core==1.3.0

# torch é dependência transitiva do sentence-transformers; sem pin, pip
# resolve o wheel padrão do PyPI com todo o stack CUDA (~1.5GB extras de
# libs nvidia-*) mesmo não havendo GPU no Render, e só o import de torch já
# empurra o RSS do processo para perto/acima dos 512MB do free tier no
# startup do uvicorn. Fixando explicitamente o wheel +cpu do índice oficial
# do PyTorch evita isso.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.13.0+cpu
sentence-transformers==5.4.1
transformers==5.5.4
huggingface-hub==1.11.0

pypdf==6.10.2
numpy==2.4.4
pandas==3.0.2
tabulate==0.10.0

groq==1.2.0

python-multipart==0.0.6
psutil==5.9.6
